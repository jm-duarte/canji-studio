import { siteSettings } from "./siteSettings";
import { homePageContent } from "./homePage";
import { project } from "./project";

export const schemaTypes = [siteSettings, homePageContent, project];
